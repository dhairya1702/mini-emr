/**
 * Where early-access submissions go.
 * Demo mode: submissions are simulated locally (no network).
 * To go live later, set this to your form endpoint (e.g. a Formspree URL
 * or your own API) and the form will POST { name, email, clinic, role }.
 */
export const ACCESS_ENDPOINT: string | null = null;
